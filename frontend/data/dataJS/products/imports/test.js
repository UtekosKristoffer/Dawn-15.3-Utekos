function showArrayData(images){
        
    let arrayImage = ``
    const vaultImageArray = arrayImages(images){
    arrayImage = ``
    }
    vaultImages.img = arrayImage
}

showArrayData(productWithVaultVariantDataArrays)

function showArrayData(images){
        
    let arrayImage = ``
    const vaultImageArray = getVaultImagesArray(images){
    }
    vaultImages.MediaImg = arrayImage
}

showArrayData(productWithVaultVariantDataArrays)

